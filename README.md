# SmartCater Prototype (CLI)

This repository contains a small CLI prototype for SmartCater. The prototype focuses on searching meals by name, ingredient, or category using in-memory sample data.

## How to run
1. Make sure you have Python 3 installed.
2. In the repository folder, run:

```bash
python3 smart_cater.py
